/** Matches Backend StudyDocumentResponse. */
export interface StudyDocument {
  id: number;
  subject_id: number;
  created_by: number;
  title: string;
  file_path: string;
  file_type: string;
  created_at: string;
}
